How to ensure that no security credentials are ever committed to the code repository ? and this
- Set up git-secrets as a pre-commit hook. If security credentials are found, block the commit from being made. 

